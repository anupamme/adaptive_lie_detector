torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
bitsandbytes>=0.41.0
numpy>=1.24.0
pandas>=2.0.0
# PINNED, not a lower bound: GroupKFold fold composition changes between
# versions, which moves four grouped-5-fold cells by 2-10 pp (docs/
# PREREG_EXP_R1D.md §10). Stratified 5-fold, LOO and every permutation p are
# version-stable. The paper prints the 1.8.0 values, so this is the version a
# reader needs to reproduce the printed digits.
scikit-learn==1.8.0
openai>=1.0.0  # For interrogator if using API
anthropic>=0.18.0  # For interrogator if using Claude
tqdm>=4.65.0
python-dotenv>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
pytest>=7.4.0  # For testing